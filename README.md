# code-rush
## I am planed to uploading the cods for the all the possible topic problems one by one here which it learned day by day
### ex1:Twosum
    ->Here just inputting the array of the n elements with target.
    ->The out put will be the location of the elements which will cauz the sum.
    ->Used some loops ,condition and checking by equating.

### Greatest number:
->Its on the example of the kids with candies just used the brute force with for loop and condition.

### Merge string:
->Merge the strings using the for loop.
->If two string are same length just merge it.
->If they are not having the same length then merge until length same then leave the remaining letters as it is.


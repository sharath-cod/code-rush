#include <stdio.h>

int main() {
    int target,nums[10], i, j, n, m;
    printf("Enter the number of elements: ");
    scanf("%d", &n);
    printf("Enter the elements: ");
    for (i = 0; i < n; i++) {
        scanf("%d", &nums[i]);
    }
    printf("Enter the target number: ");
    scanf("%d", &target);
    m = n;
    for (i = 0; i < n; i++) {
        for (j = i+1; j < n; j++) {
            if (nums[i] + nums[j] == target) {
                nums[0]=i;
                nums[1]=j;
            printf("[ %d , %d ]",nums[0],nums[1]);
            }
        }
    }
    return 0;
}
